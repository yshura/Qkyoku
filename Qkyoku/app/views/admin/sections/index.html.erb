<div class='col-md-4 mt-4 mb-3'  style='display:flex; flex-wrap:wrap;'>
  <h2>工程一覧・追加</h2>
</div>
<p><%= render 'create', section: @section %></p>
    <div class='row'>
      <div class="col-lg-5">
         <table class="table" style="font-size: 13pt; line-height: 100%;">
           <thead>
            <tr align="center">
             <th bgcolor="#f5f5f5">工程</th>
             <th bgcolor="#f5f5f5"></th>
            </tr>
          </thead>
　        <tbody>
            <!--工程名表示-->
            <% @sections.each do |section| %>
              <tr><th><%= section.name %></th><td><%= link_to "編集", edit_admin_section_path(section.id), class: "btn btn-success btn-sm" %></td></tr>
　          <% end %>
          </tbody>
         </table>
      </div>
    </div>