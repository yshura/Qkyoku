<div class='container'>
  <div class='row'>
    <ul class='table col-md-2 mt-5' style='font-size: 13pt; list-style: none;'>
        <% @sections.each do |section| %>
          <li>
              <%= link_to public_section_path(section) do %>
                <%= section.name %>
              <% end %>
          </li>
        <% end %>
    </ul>
    <div class='col-md-8 offset-md-1 mt-5'><%= image_tag 'kouteizu.jpg', class:"img-fluid" %></div>
  </div>
</div>